<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_export"
        android:title="Export CSV"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_day_start"
        android:title="Set Day Start Hour"
        app:showAsAction="never"/>

    <item android:id="@+id/action_dark_mode"
        android:title="Toggle Dark Mode"
        app:showAsAction="never"/>

    <item android:id="@+id/action_categories"
        android:title="Manage Categories"
        app:showAsAction="never"/>

    <item android:id="@+id/action_permission"
        android:title="Grant Permission"
        app:showAsAction="never"/>

</menu>
